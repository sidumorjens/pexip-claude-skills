# Built skill packages
*.skill
dist/
output/
build/

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Python (if scripts grow)
__pycache__/
*.pyc
.venv/
venv/

# Node (if scripts grow)
node_modules/

# Local install symlinks / scratch
.claude-local/

# Per-user Claude Code settings (machine-specific permission allowlists, etc).
# .claude/settings.json is intentionally NOT ignored — it's the shared/committed
# project settings file when one exists.
.claude/settings.local.json
