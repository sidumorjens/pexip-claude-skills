# /api/admin/configuration/v1/upgrade_request/<id>/finish/

**Endpoint:** `/api/admin/configuration/v1/upgrade_request/<id>/finish/`

## Fields

| Field | Type | Description |
|---|---|---|
| `validate_only` | boolean | Set to true to only validate the upgrade package without performing the upgrade. |
